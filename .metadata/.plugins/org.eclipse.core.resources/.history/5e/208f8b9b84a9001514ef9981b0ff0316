import java.util.ArrayList;

public class Parser {
	
	private static char[] symbols = {'(', ')', '{', '}', '=', '+', '-', '*', '/'};
	private ArrayList<String> tokens = new Lexer().lex(new Input("test.ink").readFile());
	
	public static void main (String[] args) {
		
	}
}
